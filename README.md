# 🏀 AutoBasket Betting Intelligence

Автономная система анализа баскетбольных матчей с интеллектуальным управлением ставками.

## 📋 Содержание

1. [Быстрый старт](#-быстрый-старт)
2. [Структура проекта](#-структура-проекта)
3. [Пошаговая настройка](#-пошаговая-настройка)
4. [Использование](#-использование)
5. [Компоненты системы](#-компоненты-системы)
6. [Конфигурация](#-конфигурация)
7. [Dashboard](#-dashboard)
8. [FAQ](#-faq)

---

## 🚀 Быстрый старт

```bash
# 1. Перейти в директорию проекта
cd autobasket

# 2. Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или: venv\Scripts\activate  # Windows

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Скопировать и настроить .env
cp .env.example .env
# Отредактируйте .env и добавьте свои ключи

# 5. Запустить систему
python main.py

# 6. (Опционально) Запустить dashboard
streamlit run dashboard.py
```

---

## 📁 Структура проекта

```
autobasket/
├── config/
│   └── settings.py          # Все настройки системы
├── core/
│   ├── bankroll_manager.py  # Управление банкроллом (Kelly)
│   ├── prediction_engine.py # ML модели предсказаний
│   ├── elo_system.py        # Система Elo рейтингов
│   ├── value_finder.py      # Поиск value bets
│   └── backtesting.py       # Тестирование стратегий
├── data/
│   ├── database.py          # SQLite база данных
│   └── api_clients.py       # API клиенты (NBA, Odds)
├── utils/
│   ├── notifications.py     # Telegram/Discord алерты
│   └── discipline.py        # Контроль дисциплины/тильта
├── main.py                  # Главный модуль
├── dashboard.py             # Streamlit dashboard
├── requirements.txt         # Зависимости
├── .env.example             # Пример переменных окружения
└── README.md                # Эта инструкция
```

---

## 📝 Пошаговая настройка

### Шаг 1: Установка Python

Убедитесь, что установлен Python 3.9+:
```bash
python --version
# Python 3.9.x или выше
```

### Шаг 2: Создание виртуального окружения

```bash
# Создаем
python -m venv venv

# Активируем (Linux/Mac)
source venv/bin/activate

# Активируем (Windows)
venv\Scripts\activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

**Важно**: Если возникают ошибки с ML библиотеками:
```bash
# Минимальная установка без ML
pip install numpy pandas requests sqlalchemy streamlit plotly python-dotenv

# Полная установка с ML
pip install numpy pandas scikit-learn xgboost requests sqlalchemy streamlit plotly
```

### Шаг 4: Настройка API ключей

#### 4.1 Odds API (рекомендуется)

1. Зарегистрируйтесь на https://the-odds-api.com/
2. Получите бесплатный API ключ (500 запросов/месяц)
3. Добавьте в `.env`:
   ```
   ODDS_API_KEY=your_key_here
   ```

#### 4.2 Telegram уведомления (опционально)

1. Напишите @BotFather в Telegram
2. Создайте бота командой `/newbot`
3. Получите токен и chat_id
4. Добавьте в `.env`:
   ```
   TELEGRAM_TOKEN=your_bot_token
   TELEGRAM_CHAT_ID=your_chat_id
   ```

### Шаг 5: Инициализация базы данных

База данных создается автоматически при первом запуске.

```bash
python main.py
# Будет создан файл autobasket.db
```

---

## 🎮 Использование

### Основные команды

```bash
# Запуск системы в интерактивном режиме
python main.py

# Запуск веб-интерфейса
streamlit run dashboard.py

# Тестирование компонентов
python -m pytest tests/
```

### Режимы работы

**1. Симуляция (по умолчанию)**
- Виртуальные деньги
- Безопасно для обучения
- Установлен по умолчанию

**2. Реальный режим (ОСТОРОЖНО!)**
- Измените в `.env`:
  ```
  SIMULATION_MODE=False
  ```
- Используйте только после тестирования!

---

## 🔧 Компоненты системы

### 1. Bankroll Manager
Управление капиталом с критерием Келли.

```python
from core.bankroll_manager import BankrollManager

bm = BankrollManager(initial_bankroll=200.00)

# Рассчитать размер ставки
amount, details = bm.calculate_optimal_bet_size(
    confidence=0.65,  # Наша уверенность
    odds=1.85,        # Коэффициент
    category=BetCategory.VALUE
)
print(f"Рекомендуемая ставка: ${amount:.2f}")
```

### 2. Prediction Engine
ML предсказания исходов матчей.

```python
from core.prediction_engine import BasketballPredictor, GameFeatures

predictor = BasketballPredictor()

features = GameFeatures(
    game_id=1001,
    home_team="Lakers",
    away_team="Warriors",
    game_date=datetime.now(),
    home_net_rating=7.3,
    away_net_rating=5.5,
    home_elo=1650,
    away_elo=1620,
    # ... другие признаки
)

prediction = predictor.predict(features)
print(f"Прогноз: {prediction.predicted_winner} ({prediction.confidence:.1%})")
```

### 3. Elo Rating System
Собственная система рейтингов команд.

```python
from core.elo_system import EloRatingSystem

elo = EloRatingSystem()

# Предсказание матча
pred = elo.predict_game("Lakers", "Warriors")
print(f"Вероятность Lakers: {pred['home_win_prob']:.1%}")

# Обновление после игры
elo.update_ratings(
    game_id=1001,
    home_team="Lakers",
    away_team="Warriors",
    home_won=True,
    margin=8
)
```

### 4. Value Bet Finder
Поиск ставок с положительным EV.

```python
from core.value_finder import ValueBetFinder

finder = ValueBetFinder(min_value_threshold=0.05)

value_bets = finder.find_value_bets(
    predictions=predictions_dict,
    market_odds=odds_dict,
    game_info=games_dict
)

for vb in value_bets:
    print(f"{vb.team_name}: EV={vb.expected_value:.1%}, Edge={vb.edge:.1%}")
```

### 5. Backtesting
Тестирование стратегий на исторических данных.

```python
from core.backtesting import StrategyBacktester, KellyStrategy

strategy = KellyStrategy(fraction=0.5, min_confidence=0.55)
backtester = StrategyBacktester(strategy, initial_bankroll=200)

result = backtester.run(
    historical_data=df,
    predictions=predictions,
    odds_data=odds
)

print(f"ROI: {result.roi:.1f}%")
print(f"Win Rate: {result.win_rate:.1%}")
print(f"Max Drawdown: {result.max_drawdown:.1%}")
```

---

## ⚙️ Конфигурация

Все настройки в `config/settings.py`:

### Bankroll Settings
```python
initial_bankroll = 200.00      # Начальный банкролл
min_bet = 5.00                 # Минимальная ставка
max_bet_percentage = 0.10      # Макс 10% от банкролла
stop_loss_daily = -0.15        # Stop-loss -15%/день
take_profit_daily = 0.20       # Take-profit +20%/день
```

### Portfolio Settings
```python
safe_allocation = 0.40         # 40% на безопасные
value_allocation = 0.35        # 35% на value bets
high_risk_allocation = 0.15    # 15% на рискованные
cash_reserve = 0.10            # 10% резерв
```

### Prediction Settings
```python
min_confidence_to_bet = 0.52   # Минимум 52% уверенности
min_value_threshold = 0.05     # Минимум 5% EV
```

---

## 📊 Dashboard

Запуск веб-интерфейса:
```bash
streamlit run dashboard.py
```

**Вкладки:**

1. **📅 Сегодня** - Игры дня, рекомендуемые ставки
2. **📈 Аналитика** - Elo рейтинги, статистика команд
3. **💰 Performance** - История ставок, ROI, equity curve
4. **⚙️ Настройки** - Параметры системы
5. **🧪 Бэктест** - Тестирование стратегий

---

## ❓ FAQ

### Q: Система гарантирует прибыль?
**A**: НЕТ! Это инструмент анализа, а не машина для печати денег. Даже с edge в долгосроке возможны значительные drawdown'ы.

### Q: Какой минимальный банкролл?
**A**: Рекомендуется $200+ для адекватного управления рисками.

### Q: Как часто обновляются коэффициенты?
**A**: Зависит от Odds API. Бесплатный план: 500 запросов/месяц.

### Q: Можно использовать для NCAA?
**A**: Система заточена под NBA, но можно адаптировать.

### Q: Что делать при losing streak?
**A**: Система автоматически:
- Предупреждает при 3+ подряд проигрышах
- Уменьшает ставки при 5+ проигрышах
- Блокирует ставки при тильте

---

## ⚠️ Дисклеймер

Эта система предназначена **ТОЛЬКО для образовательных целей**.

- Ставки на спорт могут привести к потере денег
- Прошлые результаты не гарантируют будущих
- Автор не несет ответственности за финансовые потери
- Играйте только на те деньги, которые готовы потерять

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи в консоли
2. Убедитесь, что `.env` настроен правильно
3. Проверьте версии зависимостей

---

## 📜 Версия

**v1.0.0** - Январь 2026

**Что включено:**
- ✅ Bankroll Management (Kelly Criterion)
- ✅ ML Prediction Engine
- ✅ Elo Rating System
- ✅ Value Bet Finder
- ✅ Backtesting Framework
- ✅ Portfolio Management
- ✅ Notification System
- ✅ Discipline/Tilt Detection
- ✅ Streamlit Dashboard
- ✅ SQLite Database

**В разработке:**
- 🔄 Live betting adjustments
- 🔄 Spread/Totals predictions
- 🔄 Line movement tracking
- 🔄 Multi-sport support
